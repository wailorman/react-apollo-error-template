{
  "private": true,
  "name": "write-fragment-error",
  "version": "0.1.0",
  "devDependencies": {
    "gh-pages": "^1.0.0",
    "react-scripts": "1.0.16"
  },
  "homepage" : "http://wailorman.github.io/write-fragment-error",
  "dependencies": {
    "apollo-cache-inmemory": "^1.0.0",
    "apollo-client": "^2.0.1",
    "apollo-link": "^1.0.0",
    "apollo-server-express": "^1.2.0",
    "body-parser": "^1.18.2",
    "express": "^4.16.2",
    "graphql": "^0.11.7",
    "graphql-tag": "^2.5.0",
    "graphql-tools": "^2.7.2",
    "react": "^16.0.0",
    "react-apollo": "2.0.0",
    "react-dom": "^16.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "deploy": "PUBLIC_URL=/write-fragment-error/$(git symbolic-ref --short HEAD) npm run build && gh-pages-clean && gh-pages -d build --dest $(git symbolic-ref --short HEAD) --remote"
  }
}
